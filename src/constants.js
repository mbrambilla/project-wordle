export const NUM_OF_GUESSES_ALLOWED = 6;

// Just for extra fun - let's play with word length
// Possible todo: look into making this switchable by user
// Going to need style tweaks too
//
// Valid word length is 4 or 5
// Also must change import in `src/component/Game/Games.js` to be either
// - import { WORDS_4 as WORDS } from '../../data';
// - import { WORDS_5 as WORDS } from '../../data';
export const NUM_WORD_LENGTH = 5;
